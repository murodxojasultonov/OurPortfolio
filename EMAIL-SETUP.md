# 📧 Настройка отправки контактной формы

Форма контакта готова! Теперь нужно активировать реальную отправку email.

## 🚀 Быстрый старт (ключи EmailJS уже добавлены!)

**Ваши ключи EmailJS уже в коде!** Чтобы активировать:

1. Откройте `src/components/Contact.jsx`
2. **Раскомментируйте** блок EmailJS (удалите `/*` на строке 33 и `*/` на строке 57)
3. **Закомментируйте** демо-код setTimeout (строки 25-31)
4. Сохраните → готово! ✅

Подробные инструкции ниже ⬇️

---

## 🎯 Два варианта (оба бесплатные):

---

## Вариант 1: EmailJS (Рекомендуется) ⭐

### Преимущества:
- ✅ Полностью бесплатно (200 писем/месяц)
- ✅ Не нужен backend
- ✅ Работает из браузера
- ✅ Настраивается за 5 минут
- ✅ Поддержка шаблонов

### Настройка:

#### Шаг 1: Регистрация
1. Перейдите на https://www.emailjs.com/
2. Нажмите **"Sign Up"**
3. Зарегистрируйтесь (можно через Google)

#### Шаг 2: Добавьте Email сервис
1. В Dashboard нажмите **"Add New Service"**
2. Выберите **Gmail** (или другой)
3. Подключите свой email
4. Скопируйте **Service ID**

#### Шаг 3: Создайте шаблон
1. Перейдите в **"Email Templates"**
2. Нажмите **"Create New Template"**
3. Используйте этот шаблон:

**Subject:**
```
Новое сообщение от {{from_name}}
```

**Content:**
```
От: {{from_name}}
Email: {{from_email}}

Сообщение:
{{message}}
```

4. Сохраните и скопируйте **Template ID**

#### Шаг 4: Получите Public Key
1. Перейдите в **"Account"** → **"General"**
2. Скопируйте **Public Key**

#### Шаг 5: Обновите код

✅ **Ваши ключи уже добавлены в код!**

Откройте `src/components/Contact.jsx` - вы увидите, что ключи уже заполнены (строка ~44-46):
```javascript
service_id: 'service_f2n16el',      // ✅ Уже настроено
template_id: 'template_pohml9q',    // ✅ Уже настроено
user_id: 'gPYJisRNmBkCDslk8',       // ✅ Уже настроено
```

**Что нужно сделать:**

1. **Раскомментируйте блок EmailJS** (строки 33-57)
   - Удалите `/*` в начале блока (строка 33)
   - Удалите `*/` в конце блока (строка 57)

2. **Закомментируйте или удалите демо-код** (строки 25-31)
   - Это временный код, который показывает успех без реальной отправки
   - Удалите или закомментируйте этот блок:
   ```javascript
   // setTimeout(() => {
   //   setSubmitStatus('success')
   //   setFormData({ name: '', email: '', message: '' })
   //   setIsSubmitting(false)
   // }, 1000)
   ```

3. **Сохраните файл** - готово!

#### Шаг 6: Проверьте EmailJS аккаунт

Убедитесь, что в вашем аккаунте EmailJS:
- ✅ Email сервис подключен (Gmail/Outlook/etc.)
- ✅ Шаблон `template_pohml9q` существует
- ✅ В шаблоне используются переменные: `{{from_name}}`, `{{from_email}}`, `{{message}}`
- ✅ Public Key `gPYJisRNmBkCDslk8` активен

✅ **Готово!** Форма будет отправлять письма на ваш email!

---

## Вариант 2: Formspree (Еще проще) 🚀

### Преимущества:
- ✅ Максимально просто
- ✅ Бесплатно (50 писем/месяц)
- ✅ Настройка за 2 минуты
- ✅ Не нужны API ключи

### Настройка:

#### Шаг 1: Регистрация
1. Перейдите на https://formspree.io/
2. Нажмите **"Get Started"**
3. Зарегистрируйтесь

#### Шаг 2: Создайте форму
1. Нажмите **"+ New Form"**
2. Введите ваш email (куда будут приходить сообщения)
3. Скопируйте **Form ID** (выглядит как `xpznabcd`)

#### Шаг 3: Обновите код

В `src/components/Contact.jsx`:

1. **Раскомментируйте блок с Formspree** (найдите строки ~62-70):
   ```javascript
   // Вариант 2: Используя Formspree (простая альтернатива)
   // const response = await fetch('https://formspree.io/f/YOUR_FORM_ID', {
   //   method: 'POST',
   //   headers: {
   //     'Content-Type': 'application/json',
   //   },
   //   body: JSON.stringify(formData)
   // })
   ```

2. **Закомментируйте блок EmailJS** (строки 38-60)

3. **Замените `YOUR_FORM_ID`** на ваш Form ID:
   ```javascript
   const response = await fetch('https://formspree.io/f/xpznabcd', {
   ```

#### Шаг 4: Удалите временный демо-код
Удалите или закомментируйте блок setTimeout (строки ~25-31):
```javascript
// setTimeout(() => {
//   setSubmitStatus('success')
//   setFormData({ name: '', email: '', message: '' })
//   setIsSubmitting(false)
// }, 1000)
```

✅ **Готово!** Письма будут приходить на ваш email!

---

## Вариант 3: Собственный Backend

Если хотите полный контроль, можете создать свой API:

### Node.js + Express + Nodemailer:

```javascript
// server.js
const express = require('express')
const nodemailer = require('nodemailer')
const cors = require('cors')

const app = express()
app.use(cors())
app.use(express.json())

const transporter = nodemailer.createTransport({
  service: 'gmail',
  auth: {
    user: 'your-email@gmail.com',
    pass: 'your-app-password' // Не обычный пароль! App Password из Google
  }
})

app.post('/api/contact', async (req, res) => {
  const { name, email, message } = req.body
  
  try {
    await transporter.sendMail({
      from: 'your-email@gmail.com',
      to: 'your-email@gmail.com',
      subject: `Новое сообщение от ${name}`,
      text: `От: ${name}\nEmail: ${email}\n\nСообщение:\n${message}`
    })
    
    res.json({ success: true })
  } catch (error) {
    res.status(500).json({ error: 'Failed to send' })
  }
})

app.listen(3001, () => console.log('Server running on port 3001'))
```

Затем в Contact.jsx используйте:
```javascript
const response = await fetch('http://localhost:3001/api/contact', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(formData)
})
```

---

## 🎯 Что сейчас работает:

✅ **Форма полностью функциональна**
✅ **Валидация работает**
✅ **Loader показывается при отправке**
✅ **Сообщения об успехе/ошибке**
✅ **Форма очищается после отправки**

❗ **Что нужно сделать:**
- Выбрать сервис (EmailJS или Formspree)
- Зарегистрироваться
- Получить ключи
- Обновить код
- Удалить временный setTimeout

---

## 🔍 Тестирование:

После настройки:
1. Откройте http://localhost:3000/#contact
2. Заполните форму
3. Нажмите "Отправить"
4. Проверьте ваш email

---

## 💡 Рекомендация:

Для личного портфолио лучше использовать **EmailJS** или **Formspree** - они бесплатные, простые и не требуют backend сервера.

**EmailJS** - если нужны шаблоны и кастомизация
**Formspree** - если нужна максимальная простота

Оба варианта отлично работают в production! ✅

---

## 📞 Что дальше:

1. Выберите сервис
2. Следуйте инструкциям выше
3. Обновите код
4. Протестируйте
5. Готово! 🎉
