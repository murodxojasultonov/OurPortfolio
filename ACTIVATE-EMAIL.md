# ⚡ Быстрая активация EmailJS

## Ваши ключи уже в коде!

- ✅ `service_id: 'service_f2n16el'`
- ✅ `template_id: 'template_pohml9q'`
- ✅ `user_id: 'gPYJisRNmBkCDslk8'`

---

## 📝 Что нужно сделать (2 минуты):

### 1. Откройте файл `src/components/Contact.jsx`

### 2. Найдите строку 33 и удалите `/*`

**Было:**
```javascript
    }, 1000)

    /* 
    // РАСКОММЕНТИРУЙТЕ ЭТОТ КОД ПОСЛЕ НАСТРОЙКИ EmailJS или Formspree
```

**Должно быть:**
```javascript
    }, 1000)

    
    // РАСКОММЕНТИРУЙТЕ ЭТОТ КОД ПОСЛЕ НАСТРОЙКИ EmailJS или Formspree
```

### 3. Найдите строку 57 и удалите `*/`

**Было:**
```javascript
      setIsSubmitting(false)
    }
    */
  }
```

**Должно быть:**
```javascript
      setIsSubmitting(false)
    }
    
  }
```

### 4. Закомментируйте демо-код (строки 25-31)

**Было:**
```javascript
    // Временное решение (демо режим)
    // Показываем успешную отправку после короткой задержки
    setTimeout(() => {
      setSubmitStatus('success')
      setFormData({ name: '', email: '', message: '' })
      setIsSubmitting(false)
    }, 1000)
```

**Должно быть:**
```javascript
    // Временное решение (демо режим)
    // Показываем успешную отправку после короткой задержки
    /*
    setTimeout(() => {
      setSubmitStatus('success')
      setFormData({ name: '', email: '', message: '' })
      setIsSubmitting(false)
    }, 1000)
    */
```

### 5. Сохраните файл

### 6. Проверьте на сайте

Форма теперь будет отправлять реальные письма на ваш email! 🎉

---

## ⚠️ Важно

Убедитесь, что в вашем аккаунте EmailJS:
- Email сервис подключен (Gmail/Outlook)
- Шаблон с ID `template_pohml9q` существует
- В шаблоне есть переменные: `{{from_name}}`, `{{from_email}}`, `{{message}}`

---

## 🐛 Если не работает

1. Откройте консоль браузера (F12)
2. Заполните форму и нажмите "Отправить"
3. Посмотрите ошибки в консоли
4. Проверьте, что все ключи правильные
5. Убедитесь, что в EmailJS подключен email сервис
